#include "HashTables.h"
#include <unordered_map>

using namespace std;

//unordered_map <string, int> numTable = { {"one", 1} };
//unordered_map <string, int> idTable;
//unordered_map <string, int> reservedTable;
//
//auto val = numTable["one"];

//cout << val.first;
//cout << " : " << val.second << ";" << endl;